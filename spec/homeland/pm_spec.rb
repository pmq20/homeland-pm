require 'spec_helper'

describe Homeland::Pm do
  it 'has a version number' do
    expect(Homeland::Pm::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
